<?php

namespace modava\article\models\table;

use cheatsheet\Time;
use modava\article\models\query\ArticleTypeQuery;
use Yii;


class ArticleTypeTable extends \yii\db\ActiveRecord
{
    const STATUS_PUBLISHED = 1;
    const STATUS_DISABLED = 0;

    /**
     * {@inheritdoc}
     */
    public static function tableName()
    {
        return 'article_type';
    }

    public static function find()
    {
        return new ArticleTypeQuery(get_called_class());
    }

    public static function getAllArticleType($lang = null)
    {
        $cache = Yii::$app->cache;
        $key = 'redis-get-all-article-type-' . $lang;

        $data = $cache->get($key);

        if ($data === false) {
            $data = self::find()->where(['language' => $lang])->published()->sortDescById()->all();
            $cache->set($key, $data, Time::SECONDS_IN_A_MONTH);
        }

        return $data;
    }

    public function afterDelete()
    {
        $cache = Yii::$app->cache;
        $lang = $this->language;
        $key = 'redis-get-all-article-type-' . $lang;
        $cache->delete($key);
        return parent::beforeDelete(); // TODO: Change the autogenerated stub
    }

    public function afterSave($insert, $changedAttributes)
    {
        $cache = Yii::$app->cache;
        $lang = $this->language;
        $key = 'redis-get-all-article-type-' . $lang;
        $cache->delete($key);
        parent::afterSave($insert, $changedAttributes); // TODO: Change the autogenerated stub
    }
}
